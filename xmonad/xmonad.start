#!/bin/bash

# put this file in /usr/local/bin/xmonad.start
# and change /usr/share/xsessions/xmonad.desktop to
# Exec=xmonad.start

dropbox start &

setxkbmap -option 'compose:ralt' &

# exec xmonad
exec $HOME/.cabal/bin/xmonad
